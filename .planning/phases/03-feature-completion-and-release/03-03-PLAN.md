---
phase: 03-feature-completion-and-release
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - DCTL-01
  - DOCS-01
must_haves:
  truths:
    - "`bcdl --help` works from a pipx-installed wheel and shows all flags including --format"
    - "The wheel built by `uv build --wheel` contains the correct entry point"
  artifacts: []
  key_links:
    - from: "dist/bcdl-0.1.0-py3-none-any.whl"
      to: "bcdl CLI entry point"
      via: "pipx install"
      pattern: "bcdl --help"
---

<objective>
Verify the package builds, installs, and runs correctly from a wheel via pipx. This is the final smoke test for the phase.

Purpose: Confirm the user-facing install path works end-to-end before marking Phase 3 complete.
Output: Verified working wheel install. No new files created.
</objective>

<execution_context>
@/Users/jolilius/home/src/bcdl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jolilius/home/src/bcdl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-feature-completion-and-release/03-CONTEXT.md
@pyproject.toml

<interfaces>
From pyproject.toml:
```toml
[project.scripts]
bcdl = "bcdl:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build wheel and verify contents</name>
  <read_first>
    - pyproject.toml (entry point and build system config)
  </read_first>
  <files></files>
  <action>
Run `uv build --wheel --clear` in the project root. This clears any stale wheels in `dist/` and builds a fresh one.

Verify the wheel was created: `ls dist/bcdl-0.1.0-py3-none-any.whl`

Inspect entry point in the wheel: `unzip -p dist/bcdl-0.1.0-py3-none-any.whl bcdl-0.1.0.dist-info/entry_points.txt` should show `bcdl = bcdl:main`.

Verify bcdl.py is included: `unzip -l dist/bcdl-0.1.0-py3-none-any.whl | grep bcdl.py`
  </action>
  <verify>
    <automated>cd /Users/jolilius/home/src/bcdl && uv build --wheel --clear 2>&1 | tail -3 && test -f dist/bcdl-0.1.0-py3-none-any.whl && echo "wheel exists" && unzip -p dist/bcdl-0.1.0-py3-none-any.whl bcdl-0.1.0.dist-info/entry_points.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dist/bcdl-0.1.0-py3-none-any.whl && echo "exists"` prints "exists"
    - `unzip -p dist/bcdl-0.1.0-py3-none-any.whl bcdl-0.1.0.dist-info/entry_points.txt` contains `bcdl = bcdl:main`
    - `unzip -l dist/bcdl-0.1.0-py3-none-any.whl | grep bcdl.py` shows bcdl.py is in the wheel
  </acceptance_criteria>
  <done>Wheel builds successfully and contains bcdl.py with correct entry point.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify pipx install and bcdl --help</name>
  <read_first>
    - pyproject.toml (version number for wheel path)
  </read_first>
  <files></files>
  <action>
CHECKPOINT: Human verifies the pipx install path works end-to-end.

What was built: Wheel at `dist/bcdl-0.1.0-py3-none-any.whl` containing the complete bcdl tool with --format flag, all CLI flags, and entry point.

Steps for the user:
1. Install from wheel: `pipx install --force dist/bcdl-0.1.0-py3-none-any.whl`
2. Run: `bcdl --help`
3. Verify the output shows:
   - `username` positional argument
   - `--cookies FILE` option
   - `--delay SECONDS` option
   - `--export-csv FILE` option
   - `--format FORMAT` option with format list
4. Clean up: `pipx uninstall bcdl`

Resume signal: Type "approved" if --help shows all flags correctly, or describe any issues.
  </action>
  <verify>User confirms bcdl --help output shows all 5 flags (--cookies, --delay, --export-csv, --format, username)</verify>
  <done>`bcdl --help` from pipx-installed wheel shows all flags including --format. User approved.</done>
</task>

</tasks>

<verification>
- Wheel builds without errors
- Entry point `bcdl = bcdl:main` present in wheel metadata
- `bcdl --help` shows all flags from a clean pipx install
</verification>

<success_criteria>
- `uv build --wheel` succeeds
- `pipx install dist/bcdl-0.1.0-py3-none-any.whl` installs without errors
- `bcdl --help` shows username, --cookies, --delay, --export-csv, and --format
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-completion-and-release/03-03-SUMMARY.md`
</output>
